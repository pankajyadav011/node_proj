// Build a url shortening service using express and mongodb.
// POST/shortURL accept a long url and return a shortened url.
// get/:shortID reditrect to the original url and increment access count 
// patch/:shortID allow updating the long url or acess count;